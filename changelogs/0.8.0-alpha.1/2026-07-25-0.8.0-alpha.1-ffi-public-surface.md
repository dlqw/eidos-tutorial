# Eidos 0.8 FFI tutorial surface

- Migrated FFI examples and the raylib snake walkthrough to explicit `std.Ffi` imports and the public generic pointer/string/callback APIs.
- Replaced legacy source-level `link` directives with structured extern library identities declared by `eidos.toml [ffi].libraries`, and removed the duplicate stale snake binding module.
- Removed the obsolete generated `raylib_ffi.eidos` snapshot; current generated bindings belong in a binding package with manifest-owned native inputs instead of the tutorial source tree.
- Marked cross-module effect declarations and functions `export` in the nested qualified-path example, matching default-private module visibility.
