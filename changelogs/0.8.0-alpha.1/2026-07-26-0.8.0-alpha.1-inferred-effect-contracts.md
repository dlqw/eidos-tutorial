# Inferred effect contracts

- Functions with bodies now infer an effective effect row when `need` is omitted.
- Explicit `need` remains a checked upper bound and is required for bodyless trait and external declarations.
- Documented LSP virtual `need ...` hints, materialization actions, and the role of custom domain effects.
