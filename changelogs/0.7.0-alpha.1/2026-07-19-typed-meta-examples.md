# Typed metaprogramming examples

- Updated the const-generic tutorial to use a named instance instead of the removed function-level `impl` clause.
- Reworked the reflection/derive example to use the compiler-managed `meta.Type -> meta.Items` protocol.
