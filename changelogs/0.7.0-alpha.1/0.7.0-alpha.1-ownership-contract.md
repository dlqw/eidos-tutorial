# Ownership contract and loan summaries

- Document signature-derived `ByValue` / `SharedBorrow` / `MutableBorrow` semantics and the separation between ownership contracts and body-derived loan summaries.
- Document structured Copy verification, explicit Clone, partial move/reinitialization, reborrow provenance, and drop-once behavior.
- Mark legacy `@borrow` as migration input that blocks atomically when definition and call-site ownership cannot be proven equivalent; migration never inserts `clone`.
