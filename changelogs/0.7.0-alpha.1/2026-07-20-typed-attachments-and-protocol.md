# Synchronize typed attachments and compiler-managed protocols

- Update English and Chinese BNF/README guidance from `Meta.DeriveInput -> Meta.Expansion` and legacy attributes to `meta.Type -> meta.Items`, `@[expand(...)]`, and structured `@[extern(...)]` declarations.
- Migrate tutorial derive, FFI, Raylib, and Snake examples to typed declaration tags and explicit `need ffi` signature components.
