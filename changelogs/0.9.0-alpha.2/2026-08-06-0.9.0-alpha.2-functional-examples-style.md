# Functional example style

- Reworked the standard-library, Functor/Applicative/Monad, Traversable, and principled `do` examples around pipelines, composition, fluent container chains, functional operators, and `Option` coalescing.
- Documented the boundary between derived functional code and the small set of mutation-bearing implementation kernels.
- Extended the example verification gate to prevent derived sequence transforms from regressing to nested prefix-call style and to retain coverage of the canonical functional forms.

The tutorial continues to describe Eidos `0.9.0-alpha.1`; this fragment targets the next immutable prerelease without changing the current authoritative version source.
