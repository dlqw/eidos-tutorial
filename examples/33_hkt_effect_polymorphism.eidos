Box[A] :: type {
    Wrap:: type(A)
}

audit :: effect;

apply[A, B, E: effects] :: (A -> B need E) -> A -> B need E
{
    f => value => f(value)
}

record :: Box[Int] -> Box[Int] need audit
{
    value => value
}

main :: Unit -> Box[Int] need audit
{
    _ => apply(record)(Wrap(1))
}