Cap.Io :: module {
    writer :: effect;

    write :: String -> Int need writer
    {
        _ => 1
    }
}

Demo.Main :: module {
    import Cap.Io

    run_imported :: String -> Int need Io.writer
    {
        text => Io.write(text)
    }

    run_root :: String -> Int need Cap.Io.writer
    {
        text => Cap.Io.write(text)
    }

    main :: Unit -> Int need Io.writer
    {
        _ => run_imported("hello") + run_root("world")
    }
}
