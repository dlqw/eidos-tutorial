Option[T] :: type { Some:: type(T), None :: type {} }

accumulate_if_some :: Option[Int] -> Int
{
    value => {
        mut total := 0;
        while let Some(n) = value then {
            total = total + n;
        };
        total
    }
}
